# Настройка поиска судебной практики по RAG

## Обзор

Бот поддерживает поиск судебной практики через RAG (Retrieval-Augmented Generation) с использованием векторной базы данных Qdrant.

## Архитектура

- **Qdrant** - векторная база данных для хранения эмбеддингов судебных дел
- **OpenAI Embeddings** - модель text-embedding-3-large для создания векторных представлений
- **Semantic Search** - семантический поиск по косинусному сходству

## Настройка

### 1. Установка Qdrant

#### Вариант A: Docker (рекомендуется)

```bash
docker run -p 6333:6333 -p 6334:6334 \
    -v $(pwd)/qdrant_storage:/qdrant/storage:z \
    qdrant/qdrant
```

#### Вариант B: Qdrant Cloud

1. Зарегистрируйтесь на https://cloud.qdrant.io/
2. Создайте кластер
3. Получите URL и API ключ

### 2. Настройка переменных окружения

Добавьте в `.env`:

```env
# Включение RAG
RAG_ENABLED=true

# Параметры поиска
RAG_TOP_K=6                      # Количество релевантных дел
RAG_SNIPPET_CHAR_LIMIT=1200     # Макс. длина отрывка из дела
RAG_CONTEXT_CHAR_LIMIT=6500     # Общий лимит контекста

# Модель эмбеддингов
RAG_EMBEDDING_MODEL=text-embedding-3-large

# Коллекция Qdrant
RAG_COLLECTION=judicial_practice

# Подключение к Qdrant (вариант 1: URL)
RAG_QDRANT_URL=http://localhost:6333

# Подключение к Qdrant (вариант 2: host + port)
# RAG_QDRANT_HOST=localhost
# RAG_QDRANT_PORT=6333

# API ключ (для Qdrant Cloud)
# RAG_QDRANT_API_KEY=your-api-key

# Дополнительные настройки
RAG_QDRANT_GRPC=false           # Использовать gRPC
RAG_QDRANT_TIMEOUT=30.0         # Таймаут запросов (сек)
RAG_SCORE_THRESHOLD=0.7         # Порог релевантности (0-1)
```

### 3. Подготовка данных

Создайте JSONL файл с судебными делами:

```jsonl
{"text": "Текст решения суда...", "case_number": "А40-12345/2023", "court": "АС Москвы", "date": "2023-10-15", "url": "https://...", "title": "Краткое название", "region": "Москва"}
{"text": "Текст решения суда...", "case_number": "А56-78901/2023", "court": "АС СПб", "date": "2023-09-20", "url": "https://..."}
```

**Обязательные поля:**
- `text` - полный текст решения или его ключевая часть

**Опциональные поля:**
- `case_number` - номер дела (используется как ID)
- `court` - название суда
- `date` - дата решения
- `url` - ссылка на дело
- `title` - краткое название дела
- `region` - регион

Пример файла: `data/judicial_cases_example.jsonl`

### 4. Загрузка данных в Qdrant

```bash
python scripts/load_judicial_practice.py --input data/your_cases.jsonl
```

Опции:
- `--input` - путь к JSONL файлу (обязательно)
- `--batch-size` - размер батча для обработки (default: 64)

### 5. Проверка работы

1. Запустите бота
2. Нажмите кнопку "🔍 Поиск судебной практики"
3. Задайте вопрос, например: "Найди практику по взысканию неустойки с застройщика"

## Использование

### Через кнопку интерфейса

1. В главном меню нажмите **🔍 Поиск судебной практики**
2. Введите юридический вопрос
3. Бот найдёт релевантные дела и даст анализ

### Формат ответа

Ответ включает:
- Юридический анализ на основе найденных дел
- Ссылки на конкретные судебные решения
- Список использованных дел внизу сообщения

Пример:
```
📚 Использованные дела из базы:
1. Дело А40-123456/2023 • АС Москвы • 2023-10-15 • https://...
2. Дело А56-78901/2023 • АС СПб • 2023-09-20 • https://...
```

## Техническая информация

### Компоненты

- `src/core/rag/judicial_rag.py` - основной RAG сервис
- `src/core/rag/vector_store.py` - обёртка над Qdrant
- `src/core/rag/embedding_service.py` - создание эмбеддингов
- `scripts/load_judicial_practice.py` - скрипт загрузки данных

### Параметры поиска

- **RAG_TOP_K** (default: 6) - сколько наиболее релевантных дел вернуть
- **RAG_SCORE_THRESHOLD** (default: None) - минимальный порог похожести (0-1)
  - 0.9+ - почти идентичные тексты
  - 0.7-0.9 - высокая релевантность
  - 0.5-0.7 - средняя релевантность
  - <0.5 - низкая релевантность

### Ограничения контекста

- **RAG_SNIPPET_CHAR_LIMIT** (default: 1200) - максимальная длина отрывка из одного дела
- **RAG_CONTEXT_CHAR_LIMIT** (default: 6500) - общий лимит всего контекста RAG

Эти параметры важны для соблюдения лимитов токенов OpenAI API.

## Мониторинг

Логи RAG поиска:

```python
# При успешном поиске
logger.info(f"RAG found {len(rag_fragments)} relevant cases for question")

# При ошибке
logger.warning(f"RAG search failed: {rag_error}", exc_info=True)
```

## Troubleshooting

### RAG не работает

1. Проверьте `RAG_ENABLED=true` в `.env`
2. Убедитесь, что Qdrant запущен: `curl http://localhost:6333/collections`
3. Проверьте наличие коллекции: `curl http://localhost:6333/collections/judicial_practice`

### Нет результатов поиска

1. Проверьте, что данные загружены: количество точек в коллекции
2. Попробуйте снизить `RAG_SCORE_THRESHOLD`
3. Увеличьте `RAG_TOP_K`

### Ошибки загрузки данных

1. Проверьте формат JSONL (каждая строка - валидный JSON)
2. Убедитесь, что поле `text` присутствует
3. Проверьте подключение к Qdrant

## Рекомендации по данным

### Качество текстов

- Используйте полные тексты решений или их резолютивные части
- Минимальная длина текста - 200 символов
- Максимальная длина - не критична, но учитывайте лимиты OpenAI

### Метаданные

Заполняйте максимум метаданных:
- **case_number** - важен для идентификации
- **url** - позволяет дать пользователю ссылку на первоисточник
- **date** - помогает отфильтровать устаревшую практику
- **region** - полезно для регионального поиска

### Структура коллекции

Для разных типов дел можно создать отдельные коллекции:
```env
RAG_COLLECTION=judicial_practice_civil     # Гражданские дела
RAG_COLLECTION=judicial_practice_arbitra   # Арбитражные дела
RAG_COLLECTION=judicial_practice_admin     # Административные дела
```

## Масштабирование

Для больших объёмов данных (>100k дел):

1. Используйте Qdrant Cloud с индексами HNSW
2. Настройте sharding:
```python
await vector_store.ensure_collection(
    vector_size=3072,
    shard_number=4  # Количество шардов
)
```

3. Включите gRPC для производительности:
```env
RAG_QDRANT_GRPC=true
```

## Обновление данных

Для добавления новых дел:
```bash
python scripts/load_judicial_practice.py --input data/new_cases.jsonl
```

Скрипт использует `upsert`, поэтому существующие дела (с тем же case_number) будут обновлены.
